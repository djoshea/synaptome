i = 1;

sg.i = i;
sg.x = ds.trainPivots.x(i);
sg.y = ds.trainPivots.y(i);
sg.z = ds.trainPivots.z(i);

ds.sgdim = [11 11 11];
ds.sgdir = 'U:\Brad\gordon 20090630 32 bit\tmp\';

for c = 1:ds.nch    
    width = max(ceil(log10(double([sg.x, sg.y, sg.z]) + 1)));
    fmat = sprintf('%%s%%s.tif %%0%dd-%%0%dd-%%0%dd.tif', width, width, width);
    fname = sprintf(fmat, ds.sgdir, ds.chlist{c}, sg.z, sg.y, sg.x);
    
    for z = 1:ds.sgdim(3)
        
    end
end